apiVersion: v1
kind: Service
metadata:
  name: {{ include "dextinity-admin.fullname" . }}
  labels:
    {{- include "dextinity-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
{{- if .Values.metrics.enabled }}
    - name: metrics
      protocol: TCP
      port: {{ .Values.metrics.containerPorts.http }}
      targetPort: metrics
{{- end }}
  selector:
    {{- include "dextinity-admin.selectorLabels" . | nindent 4 }}
